import React from 'react';
declare type Props = {
    className?: string;
    style?: React.CSSProperties;
};
export default function ServerOutline({ className, style, ...restProps }: Props): JSX.Element;
export {};
