import React from 'react';
declare type Props = {
    className?: string;
    style?: React.CSSProperties;
};
export default function ViewGridOutline({ className, style, ...restProps }: Props): JSX.Element;
export {};
