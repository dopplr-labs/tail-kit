import React from 'react';
declare type Props = {
    className?: string;
    style?: React.CSSProperties;
};
export default function ChipOutline({ className, style, ...restProps }: Props): JSX.Element;
export {};
