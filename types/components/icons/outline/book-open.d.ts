import React from 'react';
declare type Props = {
    className?: string;
    style?: React.CSSProperties;
};
export default function BookOpenOutline({ className, style, ...restProps }: Props): JSX.Element;
export {};
