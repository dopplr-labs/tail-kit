import React from 'react';
declare type Props = {
    className?: string;
    style?: React.CSSProperties;
};
export default function ViewListSolid({ className, style, ...restProps }: Props): JSX.Element;
export {};
