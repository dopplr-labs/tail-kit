import React from 'react';
declare type Props = {
    className?: string;
    style?: React.CSSProperties;
};
export default function PaperClipSolid({ className, style, ...restProps }: Props): JSX.Element;
export {};
