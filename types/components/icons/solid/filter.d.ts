import React from 'react';
declare type Props = {
    className?: string;
    style?: React.CSSProperties;
};
export default function FilterSolid({ className, style, ...restProps }: Props): JSX.Element;
export {};
