---
title: 'Select'
components:
  - name: 'Select'
    path: 'src/components/select/select.tsx'
---

import {
  HiOutlineBeaker,
  HiOutlineBookOpen,
  HiOutlineCalculator,
  HiOutlineCalendar,
  HiOutlineClipboard,
  HiOutlineDatabase,
} from 'react-icons/hi'
import { users } from 'constants/users'

# Select

Component to render **select menu**.

This component can be used either as a controlled component by passing both `value` and `onChange` props, or as an uncontrolled component.
When using as a controlled component, the `defaultValue` can be provided to set the initial selected value.

This is a basic select menu which can render a label, a icon for an option. The component can take options in the form of `OptionType` or `string`.

## Usage

The Select component can be imported using

```jsx
import { Select } from '@tail-kit/tail-kit'
```

### Default Select

<Playground scope={{ users }}>
  <Select
    placeholder="Select User"
    options={users.map(({ id, name }) => ({
      value: id,
      label: name,
    }))}
    className="w-64"
  />
</Playground>

### Select with Icon

<Playground
  scope={{
    users,
    HiOutlineBeaker,
    HiOutlineBookOpen,
    HiOutlineCalculator,
    HiOutlineCalendar,
    HiOutlineClipboard,
  }}
>
  <div className="flex space-x-4">
    <Select
      placeholder="Select User"
      options={users.map((user) => ({
        value: user.id,
        label: user.name,
        icon: <Avatar src={user.profileImage} />,
      }))}
      className="w-64"
    />
    <Select
      placeholder="Select Icon"
      className="w-64"
      options={[
        {
          value: 'beaker',
          label: 'Beaker',
          icon: <HiOutlineBeaker className="w-6 h-6" />,
        },
        {
          value: 'calculator',
          label: 'Calculator',
          icon: <HiOutlineCalculator className="w-6 h-6" />,
        },
        {
          value: 'book-open',
          label: 'Book Open',
          icon: <HiOutlineBookOpen className="w-6 h-6" />,
        },
        {
          value: 'calendar',
          label: 'Calendar',
          icon: <HiOutlineCalendar className="w-6 h-6" />,
        },
        {
          value: 'clipboard',
          label: 'Clipboard',
          icon: <HiOutlineClipboard className="w-6 h-6" />,
        },
      ]}
    />
  </div>
</Playground>

### Disabled Select

<Playground scope={{ users }}>
  <Select
    placeholder="Select User"
    options={users.map((user) => ({
      value: user.id,
      label: user.name,
    }))}
    className="w-64"
    disabled
  />
</Playground>

### Select with Clear Button

<Playground scope={{ users }}>
  <Select
    placeholder="Select User"
    options={users.map((user) => ({
      value: user.id,
      label: user.name,
      icon: <Avatar src={user.profileImage} />,
    }))}
    className="w-64"
    defaultValue={users[0].id}
    allowClear
  />
</Playground>

### Controlled Select Component

<Playground scope={{ users }}>
  {function ControlledSelectComponent() {
    const [valueSelected, setValueSelected] = React.useState(users[2].id)
    return (
      <div className="flex items-center space-x-4">
        <Select
          placeholder="Select User"
          options={users.map((user) => ({
            value: user.id,
            label: user.name,
            icon: <Avatar src={user.profileImage} />,
          }))}
          className="w-64"
          allowClear
          value={valueSelected}
          onChange={setValueSelected}
        />
        {valueSelected ? (
          <div>
            <div className="text-xs font-semibold text-blue-500">
              Selected User
            </div>
            <div className="text-sm text-gray-800">
              {users.find((user) => user.id === valueSelected).name}
            </div>
          </div>
        ) : null}
      </div>
    )
  }}
</Playground>

### Select with Disabled options

<Playground
  scope={{
    users,
    HiOutlineBeaker,
    HiOutlineBookOpen,
    HiOutlineCalculator,
    HiOutlineCalendar,
    HiOutlineClipboard,
    HiOutlineDatabase,
  }}
>
  <Select
    placeholder="Select Icon"
    className="w-64"
    allowClear
    options={[
      {
        value: 'beaker',
        label: 'Beaker',
        icon: <HiOutlineBeaker className="w-6 h-6" />,
        disabled: true,
      },
      {
        value: 'calculator',
        label: 'Calculator',
        icon: <HiOutlineCalculator className="w-6 h-6" />,
      },
      {
        value: 'book-open',
        label: 'Book Open',
        icon: <HiOutlineBookOpen className="w-6 h-6" />,
        disabled: true,
      },
      {
        value: 'calendar',
        label: 'Calendar',
        icon: <HiOutlineCalendar className="w-6 h-6" />,
      },
      {
        value: 'database',
        label: 'Database',
        icon: <HiOutlineDatabase className="w-6 h-6" />,
      },
      {
        value: 'clipboard',
        label: 'Clipboard',
        icon: <HiOutlineClipboard className="w-6 h-6" />,
        disabled: true,
      },
    ]}
  />
</Playground>

### Select with all Options Disabled

<Playground scope={{ users }}>
  <Select
    placeholder="Select User"
    className="w-64"
    options={users.map((user) => ({
      value: user.id,
      label: user.name,
      icon: <Avatar src={user.profileImage} />,
      disabled: true,
    }))}
  />
</Playground>

## Select Props

<PropsTable of="Select" />
